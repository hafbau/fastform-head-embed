// import Boundary from "./Boundary.svelte"
// import { createBoundary } from "@crownframework/svelte-error-boundary"

// // Create a proper Svelte component
// const BoundaryWrapper = {
//   $$render: (...args) => {
//     const boundary = createBoundary(Boundary);
//     return new boundary(...args);
//   }
// };

// export default BoundaryWrapper;